# Windows PowerShell script to start both backend and frontend using Docker Compose
# This is the fastest and most reliable way for daily development
# Usage: Right-click and run with PowerShell, or run: ./start-docker.ps1

docker-compose up --build

